# HTTP — ACME + redirection HTTPS pour le widget KODIVA.
# Chargé automatiquement (include conf.d/*.conf). Sans certificat SSL.

server {
  listen 80;
  listen [::]:80;
  server_name cdn.sdcreativ.com;

  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  location / {
    return 301 https://cdn.sdcreativ.com$request_uri;
  }
}
